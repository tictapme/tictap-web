#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "[pre-push] Preparing static site export"
npm run static:prepare

echo "[pre-push] Verifying committed src/ is up to date"
npm run static:verify-clean
