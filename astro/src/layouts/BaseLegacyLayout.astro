---
import HeaderEs from '../components/header/HeaderEs.astro';
import FooterEs from '../components/footer/FooterEs.astro';
import LegacyAssetBundle from '../components/head/LegacyAssetBundle.astro';
import type { LegacyBundleName } from '../lib/legacy-asset-bundles';

type Props = {
  title: string;
  bodyClass: string;
  lang?: string;
  headExtraHtml?: string;
  afterBodyHtml?: string;
  bodyItemType?: string;
  cssBundles?: LegacyBundleName[];
  jsBundles?: LegacyBundleName[];
};

const {
  title,
  bodyClass,
  lang = 'es-ES',
  headExtraHtml = '',
  afterBodyHtml = '',
  bodyItemType = 'https://schema.org/Blog',
  cssBundles = [],
  jsBundles = [],
} = Astro.props as Props;
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    {cssBundles.length > 0 && <LegacyAssetBundle bundleNames={cssBundles} />}
    <Fragment set:html={headExtraHtml} />
  </head>
  <body itemtype={bodyItemType} itemscope="itemscope" class={bodyClass}>
    <a class="skip-link screen-reader-text" href="#content" title="Ir al contenido">Ir al contenido</a>
    <div class="hfeed site" id="page">
      <HeaderEs />
      <slot />
      <FooterEs />
    </div>
    {jsBundles.length > 0 && <LegacyAssetBundle bundleNames={jsBundles} />}
    <Fragment set:html={afterBodyHtml} />
  </body>
</html>
